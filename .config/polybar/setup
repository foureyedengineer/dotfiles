# Install these dependencies
sudo apt install build-essential git cmake cmake-data pkg-config python3-sphinx libcairo2-dev libxcb1-dev libxcb-util0-dev libxcb-randr0-dev libxcb-composite0-dev python3-xcbgen xcb-proto libxcb-image0-dev libxcb-ewmh-dev libxcb-icccm4-dev
sudo apt install libxcb-xkb-dev libxcb-xrm-dev libxcb-cursor-dev libasound2-dev libpulse-dev i3-wm libjsoncpp-dev libmpdclient-dev libcurl4-openssl-dev libnl-genl-3-dev libiw-dev

# Go to https://github.com/polybar/polybar and get latest .tar release
cd ~/Downloads
tar xvf polybar-<version>.tar
cd polybar

# Do this OR...
./build.sh

# ... do this
mkdir build
cd build
cmake ..
make -j$(nproc)
sudo make install

#Then let it run until its done

# ignore these for now
sudo apt-get install build-essential git cmake cmake-data pkg-config python3-sphinx
sudo apt-get install libcairo2-dev libxcb1-dev libxcb-util0-dev libxcb-randr0-dev libxcb-composite0-dev python-xcbgen xcb-proto libxcb-image0-dev libxcb-ewmh-dev libxcb-icccm4-dev
